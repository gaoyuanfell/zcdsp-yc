import {FixedDirective} from './fixed.directive';

describe('FixedDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedDirective();
    expect(directive).toBeTruthy();
  });
});
